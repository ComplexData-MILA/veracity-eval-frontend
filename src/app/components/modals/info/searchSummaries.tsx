import styles from "../secondaryModal.module.scss";


type Props = {
  setActiveModal: (arg0: number) => void;
};


const SearchSummariesModal = ({ setActiveModal }: Props) => {


  return (
      <div className={styles.clearWrapper} onClick={()=> setActiveModal(0)}>
        <div className={styles.modalOne} onClick={e => e.stopPropagation()}>
          <div>
            <h4>What are AI Search summaries?</h4>
            <p>AI Search Summaries are concise overviews generated by AI models, particularly large language models (LLMs), in response to search queries. They synthesize key information from multiple sources to provide a direct, relevant answer. Unlike traditional search results, which display a list of links, AI search summaries offer coherent responses based on LLM output.</p>
          </div>
        </div>
      </div>
  );
}

export default SearchSummariesModal;