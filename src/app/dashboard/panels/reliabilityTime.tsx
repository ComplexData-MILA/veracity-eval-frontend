import styles from "../page.module.scss"

export default function ReliabilityOverTime() {
    return (
         <div className={styles.mockTile}>
            <h2 className={styles.title}>Reliability Score over time</h2>
        </div>
);
}